node_modules/
.next/
__pycache__/
*.pyc
.venv/
.env
.DS_Store

# OSMnx Overpass response cache — regenerated on demand
prep/cache/

# Raw inputs: large, and refetchable in one command.
data/dem.tif
data/population.tif

# Derived artifacts ARE committed (see README): hand.tif is expensive to
# compute, and the geojson/graphml let a teammate clone and run immediately.

# TS incremental build cache
web/tsconfig.tsbuildinfo
data/worldpop_ind.tif
data/*.part
# Source archive for GHS-POP — 20 MB and refetchable; population.tif is committed
data/ghs_pop_*.zip
data/nowcast/models.pkl

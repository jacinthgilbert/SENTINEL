# Heavy geospatial stack. Runs ONCE, offline, outside the API container.
rasterio==1.4.3
# HAND is implemented in prep/hydrology.py (no pysheds: avoids GPL-3.0
# and the llvmlite/numba wheel problem on some platforms).
osmnx==2.0.1
h3==4.2.1
geopandas==1.0.1
shapely==2.0.6
numpy==1.26.4
requests==2.32.3
psycopg[binary]==3.2.3
